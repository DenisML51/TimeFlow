import React from "react";
import { Container, Typography } from "@mui/material";

const Home = () => {
  return (
    <Container>
      <Typography variant="h4" mt={4}>
        Welcome to FastAPI + React App!
      </Typography>
    </Container>
  );
};

export default Home;
